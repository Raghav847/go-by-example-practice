services:
  mysql:
    restart: always
    image: mysql:9.2
    ports:
      - 3306:3306
    environment:
      - MYSQL_ROOT_PASSWORD=pass
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro